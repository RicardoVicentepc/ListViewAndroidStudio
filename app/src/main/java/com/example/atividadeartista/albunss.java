package com.example.atividadeartista;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class albunss extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_albunss);

       /* ListView lista = (ListView) findViewById(R.id.lv);

        ArrayList<Album> albums = adicionarAlbum();

        ArrayAdapter adapter = new AlbumAdapter(this, albums);

        lista.setAdapter(adapter);

        adicionarAlbum();*/
    }
        /*private ArrayList<Album> adicionarAlbum(){
            ArrayList<Album> album = new ArrayList <Album> ();

            Album x = new Album();
            x.setNomeAlbum("TOMARA QUE FUNCIONE");
            x.setImagem(R.drawable.img);
            album.add(x);
            return album;
        }*/

    }